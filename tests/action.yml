name: "Action Test"
description: "This is a test of the Synchronise Template action"
runs:
  using: "composite"
  steps:
    - id: test-action
      uses: ./
      with:
        name: "NHS England"
    - run: |
        echo "The time was ${{ steps.test-action.outputs.time }}"
      shell: bash
